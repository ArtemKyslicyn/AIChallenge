# Challenge 19 — видео

1. Раздел **MCP** (`/?shell=mcp`)
2. В каталоге видны `search`, `summarize`, `saveToFile`
3. Кнопка **Ночной бриф**
4. Три трассировки `.mcp-call` с передачей JSON
5. Карточка **Архив брифа** с путём файла

Не показывать значение `MCP_SHARED_TOKEN`.
