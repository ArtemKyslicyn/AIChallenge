# Текст видео — День 14. Инварианты

~2:00. Экран: Агенты → полоса Инварианты (архитектура / стек / решение / правило).

---

«День четырнадцатый. Инварианты — не реплики чата.

Посеять: архитектура hexagonal, стек FastAPI+React+Postgres, model_id на каждом ответе, нейтральные имена. Хранятся отдельной колонкой.

Запрос „переведи API на Django без слоёв“ — конфликт. Отказ без LLM: цитата инварианта, совпадение, обход не предлагаю.

Запрос внутри слоёв проходит. Ассистент работает в рамках заданных ограничений.»
