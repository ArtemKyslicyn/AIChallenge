# Challenge 16 — видео

1. Раздел **MCP** на стенде (`/?shell=mcp`)
2. Статус соединения `connected · mcp · aichallenge-mcp`
3. Таблица инструментов: `echo`, `time_now`, `list_stages`
4. (опционально) терминал: `run.py --stdio` — `initialized: yes` и `check: OK`

Не показывать значение `MCP_SHARED_TOKEN`.
