# Challenge 17 — видео

1. Раздел **MCP** (`/?shell=mcp`)
2. Таблица: `probe_stand`, `model_pulse`, `watch_brief` и колонка params
3. Кнопка **Проверить стенд** / **Вахта**
4. Трассировка вызова MCP и ответ агента с `model_id`
5. Локальный клиент: `stand-pulse-cli.mp4` — `pulse --stdio list` / `watch` / `schedule`

Не показывать значение `MCP_SHARED_TOKEN`.
