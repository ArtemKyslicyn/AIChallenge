# Точка возврата — 2026-09-03 (вечер)

**HEAD:** `42791da feat(web): quality column in the model ranking` · ветка `main` · дерево чистое
**Ничего не запушено и не задеплоено.** Всё локально.

Одна фраза агенту при возврате:

> «Трек наблюдаемости закрыт: фазы A–D.3 в продукте. Следующее — либо новая фича, либо
> оставшийся Tier B (MoT consistency cascade, семантический кэш) из
> `docs/superpowers/plans/2026-09-03-research-product-fit.md`.»

---

## Что закончено

| Фаза | Что живёт в продукте |
|------|----------------------|
| A | `run_traces`; `GET /lab/pareto`; `GET /sessions/{id}/traces` |
| B | `message_feedback`; POST/DELETE `/messages/{id}/feedback`; `/lab/feedback-stats`; `/lab/preference-export`; мягкий штраф роутеру |
| C | Float «Модели» (Рейтинг + Оценки), строка оценки под ответом, экспертный UX-гейт пройден, демо-скрипт, env-доки |
| D.1–D.2 | Каскад дешёвая → сильная модель, бейдж «эскалировали», доля эскалаций, миграция 005 |
| D.3 | Судья ответов (G-Eval) вне пути SSE, миграция 006, ветвление Score, колонка «Качество» |

Открытых follow-up'ов нет. Мастер-план:
[`2026-09-03-observability-routing-master.md`](./2026-09-03-observability-routing-master.md).

## D.3 — колонка качества: закрыта целиком

Подплан: [`2026-09-03-quality-column.md`](./2026-09-03-quality-column.md)
Спека: [`../specs/2026-09-03-quality-column-design.md`](../specs/2026-09-03-quality-column-design.md)

| Task | Статус | Коммит |
|------|--------|--------|
| 1. Домен качества | ✅ | `8fdb8cb` |
| 2. Разбор вердикта + сэмплер | ✅ | `4d9e8db` |
| 3. Рубрика в YAML + `LLMAnswerJudge` | ✅ | `7bb0d3f` |
| 4. Персистентность, миграция 006, ветвление Score | ✅ | `19fe5df` |
| 5. Запуск судьи вне пути SSE | ✅ | `640f6b1` |
| 6. API (`avg_quality`, `judged_n`, `quality_score`, `quality_model_id`) | ✅ | `085b00f` |
| 6. Колонка «Качество» в Рейтинге | ✅ | `42791da` |
| 7. Верификация, env-доки, статусы | ✅ | этот коммит |

**Три правила D.3, которые нельзя потерять** (они же в спеке):
1. Пустой `JUDGE_MODEL` ⇒ фича выключена, и выключенная даёт **ровно** сегодняшнее
   поведение и ровно сегодняшний Score.
2. Судья никогда не ожидается внутри пути SSE — только через `run_shielded` со своей сессией БД.
3. Сломанный вердикт судьи — это `None`, а не `0.0`.

Четвёртое, добавленное на Task 6: **колонка рисуется только тогда, когда в окне есть хоть одна
оценка.** Колонка прочерков читалась бы как сломанная фича, а не как выключенная. Проверено
по живому стенду: при `avg_quality = null` у всех моделей скриншот Рейтинга совпадает
пиксель-в-пиксель с тем же экраном до коммитов D.3 — на 1280×900 и на 390×844.

## Состояние проверок

```
apps/api  uv run pytest -q                      → 398 passed, 40 skipped
apps/api  RUN_INTEGRATION=1 uv run pytest -q    → 438 passed (одноразовый Postgres)
apps/api  uv run ruff check src tests           → All checks passed!
apps/api  uv run mypy src                       → Success: no issues found in 61 source files
apps/web  npx tsc -b --noEmit                   → clean
apps/web  npm run lint                          → 7 warnings (унаследованная база, не расти выше)
```

Интеграционный прогон делать **только** на отдельной одноразовой базе: `tests/integration/conftest.py`
делает `TRUNCATE`, и наведённый на стенд или на дев-базу он сотрёт данные.

## Стенд для ревью

На момент записи **поднят и оставлен работать**: Postgres `55434` (контейнер `aic-ux-db`),
API `8099`, Vite `5174`. В нём наполнены обе ветки формулы — `fast-mini` (9 оценок, ≈55%),
`balanced-pro` (8, ≈76%), `deep-reasoner-preview-2026-08` (8, ≈92%) выше порога
`JUDGE_MIN_RUNS`, а `tiny-draft` (1 прогон, 0 оценок) показывает прочерк и ранжируется
по успеху, как раньше.

Если снесён — поднять заново:

```bash
docker run -d --name aic-ux-db \
  -e POSTGRES_USER=uxrev -e POSTGRES_PASSWORD=uxrev-local-only -e POSTGRES_DB=uxrev \
  -p 55434:5432 postgres:16

cd apps/api
DATABASE_URL="postgresql+asyncpg://uxrev:uxrev-local-only@localhost:55434/uxrev" \
  uv run alembic upgrade head

DATABASE_URL="postgresql+asyncpg://uxrev:uxrev-local-only@localhost:55434/uxrev" \
USE_FAKE_LLM=true RUN_TRACE_ENABLED=true \
LLM_MODEL_CHAIN="fast-mini,balanced-pro,deep-reasoner-preview-2026-08,tiny-draft" \
MODEL_COST_PROXY_JSON='{"fast-mini":0.2,"balanced-pro":1.0,"deep-reasoner-preview-2026-08":4.5,"tiny-draft":0.1}' \
FEEDBACK_MIN_VOTES=2 CORS_ALLOW_ORIGINS="http://localhost:5174" \
  uv run uvicorn app.main:app --host 127.0.0.1 --port 8099 &

cd ../web && VITE_API_URL="http://localhost:8099/api/v1" npx vite --port 5174 --strictPort &
```

Наполнить данными обязательно — пустые таблицы нечего ревьюить. FakeLLM отвечает мгновенно,
поэтому `p50` и оценки правились SQL-апдейтом по `run_traces` (`total_ms`, `ttft_ms`,
`quality_score`, `quality_model_id`).

Выключенную фичу удобно проверять **не** правкой базы, а перехватом ответа `/lab/pareto`
в Playwright (`avg_quality: null`, `judged_n: 0`) — стенд остаётся наполненным.

Порты, которые были заняты чужим: **8000** (дев-compose пользователя). Не занимать.

## Чего не делать при возврате

- Не переписывать заново фазы A–D.3 — они в продукте и проверены.
- Не «чинить» контраст неактивной вкладки: находка не подтвердилась при трёх независимых
  замерах (5.69:1), правка была откачена сознательно.
- Не возвращать `feedback_wait` в чеклист: strip не показывается во время стрима вовсе,
  и это и есть выполнение H5.
- Не менять правило «переключение модели только до первого токена».
- Не показывать оценку судьи под самим ответом: колонка — про модель, а не про собеседника.
- Не деплоить и не пушить без явной просьбы.

## Открытые развилки

- **Tier B**, кроме сделанного G-Eval: семантический кэш и MoT consistency cascade остаются
  неначатыми. См. [research fit](./2026-09-03-research-product-fit.md).
- Оценка ответов в режимах ×2/×4 (per-slot rating) — сознательно не фича этого трека;
  копирайт пустого состояния сейчас честно отправляет пользователя в режим «Один».
- ~~Колонка «Качество» забирает ширину у имени модели~~ — закрыто: когда качество есть,
  колонка `N` сворачивается в `title` ячейки модели на всех ширинах. Размер выборки — это
  контекст для чисел, а не одно из них; ось ранжирования важнее.